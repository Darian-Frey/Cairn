# Cairn Roadmap

**Protocol:** CAIRN_V1
**Last updated:** 2026-04-17
**Phase:** Foundation

Tracks what's shipped, what's next, and what's still outstanding. Priority ordering follows [CLAUDE.md §10](CLAUDE.md).

---

## Completed

### Repo scaffolding

- [x] Directory structure: [src/](src/), [tools/](tools/), [schemas/](schemas/), [snapshots/](snapshots/), [capsules/](capsules/), [specs/](specs/), [tests/fixtures/](tests/fixtures/), [old_code/ltm_bridge/](old_code/ltm_bridge/), [old_code/schema_v5/](old_code/schema_v5/)
- [x] [.gitignore](.gitignore) — Python artefacts, venvs, caches, editor files, local Claude state, Cairn tmp/bak, `old_code/` excluded from tracking
- [x] [README.md](README.md) — honest framing (no deterministic-replay / benchmark claims), repo layout, dev priorities
- [x] [requirements.txt](requirements.txt) / [requirements-dev.txt](requirements-dev.txt) — `jsonschema`, `pytest`
- [x] [.venv/](.venv/) — created via `uv`, gitignored

### Priority #1 — [tools/cairn_scanner.py](tools/cairn_scanner.py)

All methods from §4 implemented, plus index wiring from §3.7.

- [x] `compute_st_h()` — module-level helper, first 16 hex of SHA-256 over canonical JSON (ST_H excluded)
- [x] `validate_schema()` + `schema_errors()` — draft-07 validation with path-prefixed error strings
- [x] `verify_integrity()` — recompute + compare
- [x] `detect_orphans()` — UV → missing DEP refs
- [x] `detect_circular_deps()` — DFS with cycle deduplication
- [x] `estimate_token_cost()` — `len(canonical_json) // 4`
- [x] `audit_risks()` — grouped by severity level
- [x] `certify_capsule()` — validates, re-hashes, writes to `capsules/`, appends to registry, updates `snapshots/index.json.capsules[]`, refuses duplicates
- [x] `validate_index()` — bidirectional check (indexed ↔ on-disk); configurable `root`
- [x] `update_index()` — upserts by file path, preserves `parent_ST_H`, records `project`/`phase`/`pct`/`tags`/`capsule`
- [x] `CapsuleError` exception class
- [x] CLI: `validate` / `integrity` / `audit` / `certify` / `index` subcommands

### Priority #2 — [schemas/cairn_v1.json](schemas/cairn_v1.json)

- [x] Draft-07 JSON Schema for CAIRN_V1 snapshots
- [x] All §3.1 fields covered (ALN/CTX/OBJ/CON/UV/RSK/DEP/BC/PAY/MR + ST_H/parent_ST_H)
- [x] D_TASK batch structure enforced (§3.6): `d_task`, `desc`, `tasks[]`, `blocking`
- [x] `uvTask` requires `id`/`obj`/`priority` (enum `p1|p2|p3`); `dep_refs[]` for orphan detection
- [x] RSK severity enum: `critical|high|medium|info` (§3.3)
- [x] Capsule conditional (§3.5): `capsule: true` requires `capsule_id`
- [x] `project` as first-class required top-level field
- [x] ST_H / parent_ST_H regex `^[0-9A-F]{16}$`
- [x] `dependency.requires[]` for circular-dep detection
- [x] `additionalProperties: true` at root (allows project-specific extensions per §7 / §11)

### Priority #3 — Test suite

- [x] [tests/conftest.py](tests/conftest.py) — `minimal_snapshot`, `clone_snapshot`, `tmp_capsule_env` fixtures; path bootstrap
- [x] [tests/test_scanner.py](tests/test_scanner.py), [tests/test_client.py](tests/test_client.py), [tests/test_handover.py](tests/test_handover.py), [tests/test_claude_md.py](tests/test_claude_md.py), [tests/test_server.py](tests/test_server.py), [tests/test_project_extensions.py](tests/test_project_extensions.py), [tests/test_init.py](tests/test_init.py) — **150 passing**, 0 skipped
- [x] Parametric JSON-parse check for legacy fixtures at [old_code/ltm_bridge/snapshots/](old_code/ltm_bridge/snapshots/)
- [x] Hand-authored CAIRN_V1 fixtures in [tests/fixtures/](tests/fixtures/) (`chaos_critical_rsk.json`, `diff_baseline.json`, `diff_phase_change.json`, `diff_no_change.json`) generated by [_generate.py](tests/fixtures/_generate.py)

**§8 priority cases — all complete:**

| # | Case | Status |
| --- | --- | --- |
| 1 | `validate_schema` passes on valid fixtures | Done |
| 2 | `detect_orphans` flags missing DEP | Done |
| 3 | `get_diff()` correctness | Done — phase/pct/risks/task-id sets + no-op path |
| 4 | Critical RSK blocks commit unless `--force` | Done |
| 5 | `certify_capsule` writes file + registry | Done |
| 6 | `--prune N` preserves capsules | Done |
| 7 | `index.json` updates on commit | Done |
| 8 | Multi-agent handover sim | Done — two-agent / three-agent / fork-detection scenarios |

### Priority #4 — `snapshots/index.json` initialisation

- [x] Seeded [snapshots/index.json](snapshots/index.json) and [capsules/registry.json](capsules/registry.json)
- [x] `CairnScanner.update_index()` implemented on the scanner
- [x] `certify_capsule()` writes to both the registry (canonical) and the index (navigation mirror)
- [x] 10 tests (`TestUpdateIndex`, `TestCapsuleIndexWiring`)
- [x] `commit_snapshot()` in the client calls `update_index()` on every successful commit

### Priority #5 — [src/cairn_client.py](src/cairn_client.py)

Evolved from [old_code/ltm_bridge/ltm_bridge_client.py](old_code/ltm_bridge/ltm_bridge_client.py).

**Kept from old code (and adapted to CAIRN_V1):**

- [x] `get_diff()` — semantic diff returning a structured `DiffReport` (task-id sets, phase, pct, risks, parent link)
- [x] `commit_snapshot()` — validates, audits, writes, calls `update_index()`, git-syncs
- [x] Critical RSK escalation with bold-red terminal alert

**Dropped:**

- [x] Clipboard watcher — removed; superseded by priority #7 IPC server

**Added:**

- [x] `--dry-run` — validate + diff without writing
- [x] `--force` — override blocking critical RSK
- [x] `--no-push` — local-only commit (also used by tests)
- [x] `--tag` — store tags on index entries
- [x] `prune(keep_n)` — last-N retention; never touches `capsules/` or any `capsule:true` file
- [x] CLI: `commit` / `diff` / `prune` subcommands
- [x] `export` / `import` subcommands — wired in priority #6

### Priority #6 — CLAUDE.md export / import

Lives in [src/claude_md.py](src/claude_md.py); CLI wired into `cairn_client.py` as `export` / `import` subcommands.

- [x] `snapshot_to_markdown()` — renders CTX / OBJ / UV / RSK / DEP / MR plus a JSON metadata header in an HTML comment (preserves `project`, `ST_H`, `parent_ST_H`, `PAY`, `CON`, `ALN`, `BC`, capsule fields)
- [x] `markdown_to_snapshot()` — parses Markdown back into a CAIRN_V1 snapshot; recomputes `ST_H` from the parsed body (hand-edits to the Markdown are reflected, not ignored)
- [x] **Byte-level round-trip fidelity** verified on all three fixtures: `ST_H` survives export → import unchanged
- [x] 19 tests (`TestExportStructure`, `TestRoundTrip`, `TestImportEdgeCases`)
- [x] CLI: `cairn_client.py export <snapshot> [-o CLAUDE.md]` / `cairn_client.py import CLAUDE.md [-o out.json]`

### Priority #7 — [src/cairn_server.py](src/cairn_server.py)

Local IPC server replacing the LTM-Bridge clipboard watcher. Stdlib-only (resolves former open decision on framework choice).

- [x] `CairnServer` class over `http.server.HTTPServer`; binds 127.0.0.1 by default
- [x] `POST /snapshot` — full commit pipeline with `force` / `dry_run` / `push` / `tags` query params
- [x] `GET /status` — latest index entry (project, ST_H, phase, pct, tags, capsule count)
- [x] `GET /health` — liveness probe
- [x] `start_in_thread()` / `shutdown()` for embedding and tests
- [x] Appropriate status codes: 200 OK, 400 (schema / integrity / bad body), 409 (blocking critical risk), 404 (unknown endpoint / no snapshots yet)
- [x] 16 tests covering all endpoints + error paths

---

### Priority #8 — Project-specific schema extensions

Per-project fragments in [schemas/projects/](schemas/projects/) auto-loaded by the scanner when `snapshot["project"]` matches a fragment filename. Base schema gained a `PROJ_EXT` container; fragments constrain its shape.

- [x] [coda.json](schemas/projects/coda.json) — pipeline status, eigenvalues, SPARC galaxy count, Krylov dim
- [x] [terra-siege.json](schemas/projects/terra-siege.json) — build phase, system completion, target platforms
- [x] [nyx-audio.json](schemas/projects/nyx-audio.json) — module list with API stability, MSRV, crate version
- [x] [lumina.json](schemas/projects/lumina.json) — module list with topic / curriculum stage, alignment
- [x] Scanner auto-loads fragments, caches validators (including negative results for unknown projects)
- [x] `schema_errors()` prefixes fragment errors with `[<project>]`
- [x] 23 tests across all four projects + unknown-project fallthrough + `projects_dir=None` escape hatch

### Specs — [specs/rehydration.md](specs/rehydration.md)

Normative specification of the four-step rehydration algorithm from CLAUDE.md §3.2.

- [x] Algorithm overview with pseudocode
- [x] Step-by-step contracts for each gate (integrity, DEP, RSK, UV) including exit conditions and override semantics
- [x] Failure-mode table mapping each detectable failure to its detection step and remedy
- [x] Diff-chain and capsule rehydration semantics
- [x] Project-extension integration (`PROJ_EXT` validated at step 1, consulted in step 4)
- [x] Explicit non-goals (no deterministic replay, no token compression, no prose truth-checking)
- [x] Cross-reference table back to CLAUDE.md sections and reference implementations

---

### [cairn/init.py](cairn/init.py) — project seeder

Seeds a fresh tree with the Cairn layout; idempotent by default (`--force` to overwrite).

- [x] Creates `snapshots/` and `capsules/`; optionally `schemas/projects/` (`--with-projects-dir`)
- [x] Seeds `snapshots/index.json` and `capsules/registry.json` with empty structures
- [x] Appends a Cairn block to `.gitignore` (or creates one); never duplicates on re-run
- [x] Optional `CLAUDE.md` stub (`--with-claude-md`)
- [x] `--with-bundled-projects` copies the four bundled fragments (coda / terra-siege / nyx-audio / lumina) into the target's `schemas/projects/`
- [x] CLI: `cairn init <project> [--target DIR] [--force] [--with-claude-md] [--with-projects-dir] [--with-bundled-projects]`
- [x] 15 tests covering fresh init, flags, idempotence, force-overwrite, and a post-init smoke check (author + commit a snapshot into the initialised tree)

### Package distribution — [pyproject.toml](pyproject.toml)

Cairn is now installable as a Python package.

- [x] Unified layout under [cairn/](cairn/) — `scanner.py`, `client.py`, `server.py`, `claude_md.py`, `init.py`, `cli.py`
- [x] Schemas bundled as package data at [cairn/schemas/](cairn/schemas/) — `cairn_v1.json` + all four project fragments
- [x] Unified `cairn` entry point via `[project.scripts]` — 11 subcommands replace the four pre-packaging scripts
- [x] Editable install verified: `uv pip install -e ".[dev]"` → `cairn --help`, `cairn init <project>`, `cairn validate ...`
- [x] Install target: `pip install git+https://github.com/Darian-Frey/Cairn.git` for consumers

### CAP-001 — "Foundation complete" (sealed 2026-04-17)

- Snapshot: [snapshots/cairn_20260417_221142_710132.json](snapshots/) — ST_H `C6E4104B81F8449A`
- Capsule:  [capsules/CAP-001.json](capsules/CAP-001.json) — ST_H `1E0AA842450DB7BB`, sealed at `2026-04-17T22:11:42Z`
- Recorded in [capsules/registry.json](capsules/registry.json) and [snapshots/index.json](snapshots/index.json)
- First real artefact in the chain; parent_ST_H = null (chain origin)

---

## Next up

- [ ] Deploy Cairn into CODA (first §11 live integration — probably its own session with a CODA-specific CLAUDE.md)
- [ ] Server hardening: auth token for cross-machine use, structured logging — deferred until there's a concrete reason

---

## Open decisions

None.

### Resolved

- **Legacy fixtures** (2026-04-15) — CAIRN_V1 is canonical; legacy payloads stay reference-only. Hand-authored CAIRN_V1 equivalents in [tests/fixtures/](tests/fixtures/).
- **`update_index()` location** (2026-04-16) — lives on the scanner, beside `validate_index()`.
- **Registry vs index relationship** (2026-04-16) — `capsules/registry.json` is the canonical capsule metadata authority; `snapshots/index.json.capsules[]` is a lightweight navigation mirror populated by `certify_capsule()`.
- **IPC server framework** (2026-04-17) — stdlib `http.server`. No flask/fastapi dep; kept the install footprint minimal (jsonschema + pytest only).
- **Capsule registry scope** (2026-04-17) — shared single-directory layout kept. In §11 deployments each project gets its own repo, so there's only ever one project's capsules per tree. Migration to per-project subdirs is mechanical if a real collision ever surfaces.

---

## Invariants (do not regress)

- No `SCHEMA_V5` naming in new code (§9)
- No clipboard watcher (§9)
- No "deterministic replay" language — always "structured rehydration" (§9)
- Never modify `old_code/` (§9)
- No whitepaper-style benchmark claims (§9)
- Capsules are permanent — `--prune` must never touch them (§3.5, §9)
- ST_H convention: first 16 hex chars of SHA-256 over canonical JSON with `ST_H` excluded (§4)
